{
  "scripts": {
    "heroku-prebuild": "npm install && cd api-server && npm install && forever start server.js",
    "start": "cd frontend && npm install && npm start"
  },
  "dependencies": {
    "forever": "^0.15.3"
  }
}
